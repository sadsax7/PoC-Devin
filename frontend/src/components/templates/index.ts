/**
 * @fileoverview Templates barrel export.
 *
 * Re-exports all template components.
 * Templates define page layouts and receive organisms as children.
 *
 * @remarks
 * FRONTEND-GUIDELINES.md §3.4 — Templates.
 * Templates contain no business logic — only layout and composition.
 *
 * @example
 * ```tsx
 * import { AuthPageTemplate, DashboardTemplate } from '@/components/templates';
 * ```
 */

// Export templates here as they are created.
// Example: export { AuthPageTemplate } from './AuthPageTemplate';
